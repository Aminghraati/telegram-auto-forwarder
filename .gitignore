# ============================================
# ⚠️⚠️⚠️ فوق‌حساس: هرگز این فایل‌ها را کامیت نکنید
# session = کلید کامل اکانت تلگرام شماست!
# ============================================
*.session
*.session-journal
*.session.bak

# QR لاگین (حاوی توکن موقت ورود است)
login_qr.png
login_qr*.png

# tdata تلگرام دسکتاپ (حاوی کلیدهای کامل اکانت است!)
tdata/
tdata_copy/
tdata_backup/

# بکاپ‌ها و نسخه‌های قدیمی سشن
*.session.bak*
*.session.old
*.session.old.*
converted_session*

# فایل‌های موقت QR و دیباگ tdata
login_qr.html
map_parts.pkl

# حالت لاگین دو مرحله‌ای
login_state.txt

# ============================================
# فایل‌های اجرایی/محلی
# ============================================
__pycache__/
*.pyc
*.pyo
*.log
run_log.txt
bot.log

# آمار و تاریخچه (اگر تولید شود)
send_stats.json
send_history.json

# ============================================
# فایل‌های سیستمی
# ============================================
.freebuff/
.DS_Store
Thumbs.db
desktop.ini

# فایل تنظیمات محلی (شماره تلفن در آن است)
# توجه: telethon_config.py الگوی کد را نگه می‌دارد؛
# اگر شماره واقعی داخلش گذاشتید، این خط را از کامنت خارج کنید:
# telethon_config.py

# محیط مجازی (اگر استفاده کردید)
venv/
.venv/
env/
