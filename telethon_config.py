"""
تنظیمات Telethon برای فوروارد خودکار پیام‌های سیو مسیج
برای دریافت API_ID و API_HASH:
1. به https://my.telegram.org برو
2. وارد اکانت خود بشو
3. Application را ایجاد کنید
4. API_ID و API_HASH را کپی کنید و اینجا بذارید
"""

# اطلاعات اپلیکیشن تلگرام
# ✅ بدون نیاز به my.telegram.org — از اعتبارنامه رسمی و عمومی Telegram Desktop
#    استفاده می‌شود (داخل سورس‌کد متن‌باز تلگرام منتشر شده و عمومی است)
API_ID = 6
API_HASH = "eb06d4abfb49dc3eeb1aeb98ae0f581e"

# اگه بعداً API شخصی گرفتی، به‌جای مقادیر بالا بذار (اختیاری):
# API_ID = 12345678
# API_HASH = "your_api_hash_here"

# نام session file (فایل لاگین روی سیستم خودت ذخیره می‌شه)
SESSION_NAME = "my_telegram"

# 📱 شماره تلگرام تو (برای لاگین غیرتعاملی با login.py)
# ⚠️ شماره خودت را اینجا بگذار — هرگز کامیت/انتشار نکن
PHONE_NUMBER = "+98XXXXXXXXXX"

# نام پوشه تلگرامی که گروه‌های هدف داخلش هستن
# (گروه‌ها را در تلگرام داخل یک پوشه به همین نام بگذار)
FOLDER_NAME = "گپ"

# منبع پیام‌ها: سیو مسیج (Saved Messages)
SAVED_MESSAGES_SOURCE = {
    # در شروع هر دور، پیام‌های سیو مسیج دوباره خوانده بشن؟
    # اینطوری هر وقت بخوای، فقط توی سیو مسیج پیام جدید بذار و بدون ری‌استارت
    # دور بعد اسکریپت اون‌ها رو فوروارد می‌کنه.
    "refresh_each_round": True,
    # چند پیام آخر سیو مسیج به‌عنوان متن‌های مرجع خوانده بشه
    "message_count": 5,
    # اگه سیو مسیج خالی بود، آخرین پیام استفاده بشه (نباید اتفاق بیفته ولی امن‌تره)
    "use_latest_message_fallback": True,
}

# 🛰️ پراکسی برای عبور از فیلترینگ (اگه مستقیم وصل نمی‌شه)
# type: "socks5" (پیش‌فرض), "http", یا "mtproto"
#
# نمونه‌های رایج:
#   v2rayN/Nekoray/گفتوگوی ویندوز: socks5 روی 127.0.0.1:10808
#   کلش/Clash Verge: socks5 روی 127.0.0.1:7890  (یا http روی همون پورت)
#   MTProto proxy: type="mtproto" + secret پراکسی
# ⚠️ پراکسی باید قبل از اجرای اسکریپت روشن (وصل) باشه
PROXY = {
    "enabled": False,   # ⚠️ اگه مستقیم وصل نمی‌شی، True کن
    "type": "socks5",
    "host": "127.0.0.1",
    "port": 10808,
    # فقط برای نوع mtproto:
    "secret": "",
}

# تنظیمات ارسال
FORWARD_CONFIG = {
    # ⏱️ تاخیر تصادفی بین هر پیام (ثانیه) — طبق خواسته شما: ۱۵ تا ۲۵
    "min_delay": 15,
    "max_delay": 25,
    # 🔀 هر دور، ترتیب گروه‌ها تصادفی باشه (الگوی غیر تکراری)
    "shuffle_groups": True,
    # ⚡ سقف زمان ارسال موازی هر پیام به همه گروه‌ها (ثانیه)
    # تعداد گروه‌ها هر چقدر باشد، ارسال هر پیام بیشتر از این طول نمی‌کشد
    "max_broadcast_seconds": 20,
}
